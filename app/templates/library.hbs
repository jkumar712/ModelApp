<div class="container navbar navbar-default">
  <ul class="nav navbar-nav">
    <li class="active">{{#link-to "library"}}Library{{/link-to}}</li>
    <li>{{#link-to "user"}}Users{{/link-to}}</li>
  </ul>
</div>
<div class="container">
  <form>
    <h4>New book</h4>
    <div class="form-group">
      <label for="book-name">Book Name</label>
      <input type="text" id="b" placeholder="Book Name">
    </div>
    <div class="form-group">
      <label for="author">Author</label>
      <input type="text" id="a" placeholder="Author Name">
    </div>
  </form>

    <!--<tbody class="table-hover" id="add">
      {{#each lib as |lib|}}
      <tr>
        <td>{{lib.SlNo}}</td>
        <td>{{lib.book}}</td>
        <td>{{lib.author}}</td>
      </tr>
      {{/each}}
    </tbody>-->

      {{#each model as |bookUnit|}}
      {{book-listing lib=bookUnit}}
      {{/each}}




</div>
{{outlet}}
